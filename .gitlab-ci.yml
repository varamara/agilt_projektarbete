stages:
  - build
  - deploy

build:
  stage: build
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - build/

deploy:
  stage: deploy
  only:
    - master  # Deploy bara från master-grenen, justera efter behov
  script:
    - echo "Deploying to production..."
